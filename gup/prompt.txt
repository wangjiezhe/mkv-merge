<purpose>

你是一个专业python程序员，现在需要编写一个程序，使用Python和mkvtoolnix，将PV01.mkv，PV01.mka（如果存在的话），以及位于dist/subsetted文件夹内的字幕和字体，封装进新的mkv中，并将输出文件放在dist文件夹下。

</purpose>

<rules>

- 字幕文件的命名为`PV01.**.ass`，其中第一部分与视频文件名相同，第二部分为语言代码（可能不存在，则此时默认为简体中文），其中ja代表日文，sc、SC代表简体中文，tc、TC代表繁体中文等。要对字幕轨道正确设置语言和地区，并用语言名称对字幕轨道进行命名。如果第二部分是`comment`的话，则代表这是一个监督评论的字幕轨道，将其命名为监督评论。
- mkv文件中可能有字幕，要保留。
- 最终的mkv文件如果仅有一个字幕轨道，将其设为字幕默认轨道；如果包括多个字幕轨道，将简体中文（非评论的）设为字幕默认轨道。
- 字体可能是otf，ttf，ttc，文件扩展名可能是大写，也可能是小写。
- mkv文件中有音频轨道，mka文件（如果存在的话）是音频轨道，在新的mkv文件中，选择声道最多的轨道作为默认音频轨道。
- 合并之后对flac音频轨道根据声道情况进行重命名，例如双声道命名为2ch，2.1声道命名为2.1ch，5.1声道命名为5.1ch。
- 合并之后对aac音频轨道（如果存在的话）进行重命名，按照默认顺序，第一个是声优评论，第二个是监督评论，第三个是军事评论。
- 如果音频轨道原有名称，则不对其进行修改，保持原样不变。
- mkv中可能有多个视频轨道，保持原样不变。

</rules>

<output>

仅输出python代码。

</output>
